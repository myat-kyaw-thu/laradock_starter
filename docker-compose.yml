name: laradoc-starter

services:

  # ── PHP 8.4-FPM ───────────────────────────────────────────────
  php:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
    container_name: laravel_php
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./src:/var/www/html                          # all projects live here
      - ./docker/nginx/dashboard:/var/www/dashboard # local dashboard
      - composer_cache:/home/laravel/.composer
    environment:
      - APP_ENV=local
    networks:
      - laravel
    depends_on:
      mysql:
        condition: service_healthy

  # ── Nginx ─────────────────────────────────────────────────────
  nginx:
    image: nginx:1.27-alpine
    container_name: laravel_nginx
    restart: unless-stopped
    ports:
      - "80:80"          # catch-all and domain routing
      - "443:443"        # ssl (optional future extension)
    volumes:
      - ./src:/var/www/html
      - ./docker/nginx/dashboard:/var/www/dashboard
      - ./docker/nginx/conf.d:/etc/nginx/conf.d      # only conf.d/ loaded — template stays outside
    networks:
      - laravel
    depends_on:
      - php

  # ── MySQL 8.0 ─────────────────────────────────────────────────
  mysql:
    image: mysql:8.0
    container_name: laravel_mysql
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootsecret}
    volumes:
      - mysql_data:/var/lib/mysql
      - ./docker/mysql/my.cnf:/etc/mysql/conf.d/my.cnf:ro
    networks:
      - laravel
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "--silent"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # ── phpMyAdmin ────────────────────────────────────────────────
  phpmyadmin:
    image: phpmyadmin:5.2
    container_name: laravel_phpmyadmin
    restart: unless-stopped
    # Ports removed: phpMyAdmin is now proxied via http://phpmyadmin.localhost
    environment:
      PMA_HOST: mysql
      PMA_PORT: 3306
      PMA_USER: root
      PMA_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootsecret}
      UPLOAD_LIMIT: 50M
    networks:
      - laravel
    depends_on:
      mysql:
        condition: service_healthy

  # ── Redis ─────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: laravel_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - laravel
    profiles:
      - extras

  # ── Mailpit ───────────────────────────────────────────────────
  mailpit:
    image: axllent/mailpit:v1.21
    container_name: laravel_mailpit
    restart: unless-stopped
    ports:
      - "1025:1025" # Keep SMTP port open for local CLI mailers
    # Port 8025 removed: Mailpit web UI is now proxied via http://mailpit.localhost
    networks:
      - laravel
    profiles:
      - extras

  # ── Composer (one-off runner) ─────────────────────────────────
  # No container_name — avoids conflict when run multiple times
  composer:
    image: composer:2.8
    working_dir: /var/www/html
    volumes:
      - ./src:/var/www/html
      - composer_cache:/tmp/composer
    environment:
      - COMPOSER_HOME=/tmp/composer
    entrypoint: ["composer"]
    networks:
      - laravel

  # ── Node / Vite (frontend — optional profile) ─────────────────
  node:
    image: node:22-alpine
    container_name: laravel_node
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./src:/var/www/html
      - node_modules:/var/www/html/node_modules
    ports:
      - "5173-5183:5173-5183" # Support multiple Vite instances on a port range
    environment:
      - VITE_HOST=0.0.0.0
    command: sh -c "[ -d node_modules ] || npm install && npm run dev"
    networks:
      - laravel
    profiles:
      - frontend

# ── Networks ──────────────────────────────────────────────────────
networks:
  laravel:
    driver: bridge

# ── Volumes ───────────────────────────────────────────────────────
volumes:
  mysql_data:
  redis_data:
  composer_cache:
  node_modules:
